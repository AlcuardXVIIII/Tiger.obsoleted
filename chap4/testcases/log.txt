     letExp(
      decList(
       typeDec(
        nametyList(
         namety(myint,
          nameTy(int)),
         nametyList(
          namety(arrtype,
           arrayTy(myint)),
          nametyList(
           namety(retype,
            recordTy(
             fieldList(
              field(name,
               string,
               TRUE),
              fieldList(
               field(age,
                int,
                TRUE),
               fieldList())))),
           nametyList())))),
       decList(
        varDec(arr1,
         arrtype,
         arrayExp(arrtype,
          intExp(10),
          intExp(0)),
         TRUE),
        decList(
         varDec(arr2,
          retype,
          recordExp(retype,
           efieldList(
            efield(name,
             stringExp("hi")),
            efieldList(
             efield(age,
              intExp(12)),
             efieldList()))),
          TRUE),
         decList(
          functionDec(
           fundecList(
            fundec(tt,
             fieldList(
              field(age,
               int,
               TRUE),
              fieldList(
               field(name,
                string,
                TRUE),
               fieldList())),
             string,
             intExp(1)),
            fundecList(
             fundec(qq,
              fieldList(),
              intExp(2)),
             fundecList()))),
          decList())))),
      expList(
       forExp(i,
        intExp(2),
        intExp(3),
        opExp(
         DIVIDE,
         intExp(5),
         intExp(9)),
        TRUE),
       expList(
        opExp(
         DIVIDE,
         intExp(3),
         intExp(4)),
        expList())))